<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="mobile_project2"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            
        <!-- ============================================== -->
        <!-- Google Maps API Key -->
        <!-- ============================================== -->
        <meta-data 
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBB1uMvd8EA2Wb_rBqWHFx1K6qqb7J5d7A" />
            
    </application>

    <!-- กำหนดสิทธิ์ให้แอปสามารถสั่งเปิดเบราว์เซอร์ และแอปภายนอกได้ -->
    <queries>
        <!-- อนุญาตให้เปิดลิงก์เว็บไซต์ HTTPS (Facebook, IG, Line, Web) -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <!-- อนุญาตให้เปิดลิงก์เว็บไซต์ HTTP -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <!-- อนุญาตให้เปิดหน้าระบบโทรศัพท์เมื่อกดเบอร์โทร -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tel" />
        </intent>
        <!-- ค่าดั้งเดิมของระบบ Flutter สำหรับประมวลผลข้อความ -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>