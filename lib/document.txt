firebase [project2] 

test การเชื่อมต่อ Firebase 
test ระบบ registor & login เบื้องต้น

test การดึงข้อมูลร้านอาหารทั้งหมดมาเป็น Stream (Real-time)
test การบันทึกการจองของร้านนี้มาเป็น Database (Firestore)
     แบ่งเป็น test ย่อยหลายอย่างเกี่ยวกับการจองร้านนี้
        จองย้อนหลังไม่ได้
        จองซ้ำเวลาเดิมไม่ได้ ?? พอเป็นแอปแล้วแค่จะขึ้นเตือน
        จองไม่ได้ถ้าเวลานั้นคนจองเต็มแล้ว
        จอง 0 คนไม่ได้

test การจองร้านโปรด 💖
test การยกเลิกโดยจะแสดงใน terminal ว่าจองอะไรไปแล้วบ้างว่าดึงมาถูกต้องแล้วค่อยลบอันแรก

แก้ไข database 
  เพิ่ม พิกัดร้าน, 
  เพิ่มรูปเป็น array,
  เพิ่ม tags สำหรับ filter,
  เพิ่มปุ่ม dummy 
  test ผ่านหมด

เพิ่มระบบให้ดาว
ทำระบบ search & แล้วก็ ดึงข้อมูลจาก Firestore มาเก็บใน ram เลยเพราะว่าจะได้ง่ายต่อการทำ search แบบไม่ต้องไปถามหาจาก firebase
 ทุกครั้งที่ search จะได้ดึงข้อมูลร้านทั้งหมดมาทีเดียวเก็บไว้ใน ram เลย